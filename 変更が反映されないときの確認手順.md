# 変更が反映されないときの確認手順

## いま見ているのは「どの環境」か

変更したコードが動くのは、**そのコードが動いている環境**だけです。

| 環境 | 変更を反映する方法 |
|------|---------------------|
| **ローカル（自分のPCで npm start）** | 下記「ローカルで確認する」を実行 |
| **本番（Vercel のURL）** | 下記「本番に反映する」を実行 |

---

## 1. ローカルで確認する（開発中）

1. **ターミナルで開発サーバーを起動している場合**
   - 一度 **Ctrl+C** で止める
   - もう一度 **`npm start`** で起動する  
   → 変更が入ったコードで動き直します。

2. **ブラウザ**
   - **強制再読み込み**する  
     - Windows: **Ctrl + Shift + R** または **Ctrl + F5**  
     - Mac: **Cmd + Shift + R**  
   → キャッシュを無視して最新のコードが読み込まれます。

3. **Cursor でファイルを保存したか**
   - 変更したファイルが **未保存**（タブに ● など）の場合は **Ctrl+S** で保存する。

---

## 2. 本番（Vercel）に反映する

本番URL（https://amazon-profit-calculator-virid.vercel.app/ など）で確認している場合は、**GitHub にプッシュしないと反映されません**。

1. **ターミナルでプロジェクトフォルダに移動**
   ```powershell
   cd c:\Users\KH060\amazon-profit-calculator
   ```

2. **変更をコミットしてプッシュ**
   ```powershell
   git add .
   git commit -m "履歴選択を1プロジェクト扱いに、月間販売額を自動計算に統一"
   git push origin main
   ```

3. **Vercel の再デプロイを待つ**
   - プッシュ後、数分で本番が自動更新されます。
   - Vercel の **Deployments** で「Ready」になれば反映済みです。

4. **本番URLを強制再読み込み**
   - ブラウザで **Ctrl + Shift + R**（または Ctrl + F5）してから再度確認する。

---

## 3. どちらの変更も効いているか確認する方法

### 履歴を「1プロジェクト」として扱う変更

1. **履歴** タブで、既存の履歴を **1件クリック** する。
2. 利益計算の **結果画面** または **詳細リサーチ** などが開く。
3. 画面上部の **「利益計算」** タブをクリックする。
4. **入力画面** に、さきほど選んだ履歴の **商品名・ASIN・原価などがそのまま** 表示されていれば反映されています。
5. 原価などを変えて **「計算する」** を押すと、**新規ではなく、その履歴1件が更新**されていればOKです。

### 月間販売額の自動入力

1. **詳細リサーチ** 画面を開く（履歴から選んでも、新規でOK判定したあとでも可）。
2. ライバル1の **販売価格（円）** に **1000**、**月間販売数** に **50** を入力する。
3. **月間販売額（円）** に **50000** が自動で入れば反映されています。
4. 履歴から開いた場合も、**販売価格** と **月間販売数** が入っているライバルは、**月間販売額** が自動で表示されていればOKです。

---

## まとめ

- **ローカルで試している** → 開発サーバーを再起動（Ctrl+C → `npm start`）し、ブラウザを **Ctrl+Shift+R** で再読み込み。
- **本番URLで試している** → 上記のとおり **git add / commit / push** してから、数分待って本番を **Ctrl+Shift+R** で再読み込み。

どちらの環境で開いているかが「反映されない」原因になっていることが多いです。
