# Firestore の内容を確認する手順（情報のシェア方法）

## アプリのコードが想定している Firestore の形

このアプリは、次のような Firestore の構造を前提に動いています。

| 項目 | 想定している内容 |
|------|------------------|
| **コレクション1** | `config` |
| **ドキュメント** | `config` コレクション内に、ドキュメントIDが **`workspace`** の1件 |
| **フィールド** | `ownerUid`（オーナーの Firebase UID） |
| **コレクション2** | `history` |
| **ドキュメント** | `history` コレクション内に、履歴1件につき1ドキュメント（IDは自動採番） |
| **履歴ドキュメントの主なフィールド** | `createdBy`, `createdAt`, `createdAtOrder`, `updatedAt`, `updatedAtOrder` のほか、利益計算で保存している項目（`asin`, `productName`, `status`, `detailResearch` など） |

---

## あなたから情報をシェアする方法（こちらで比較するため）

次のスクリーンショットを送ってもらえれば、「Firebase に上がっている情報」と「コードが想定している形」が一致しているか確認できます。**値は隠してかまいません。** コレクション名・ドキュメントID・フィールド名が写っていれば十分です。

---

### スクリーンショット1: Firestore のコレクション一覧

**撮り方：**

1. [Firebase コンソール](https://console.firebase.google.com/) を開く
2. 対象のプロジェクト（amazon-profit-calculator）を選択
3. 左メニュー **「Build」** → **「Firestore Database」** をクリック
4. **「データ」** タブで、ルート直下に表示されている **コレクション名の一覧** が写るようにスクリーンショットを撮る

**確認したいこと：**  
`config` と `history` の2つのコレクションがあるか

---

### スクリーンショット2: `config` の中身

**撮り方：**

1. Firestore の「データ」タブで、**`config`** コレクションをクリックして開く
2. その下に表示される **ドキュメント一覧**（ドキュメントIDが `workspace` など）と、**フィールド名**（例：`ownerUid`）が分かるようにスクリーンショットを撮る  
   - 値（UID の文字列など）は隠してOK

**確認したいこと：**  
`config` に `workspace` というドキュメントがあり、`ownerUid` のようなフィールドがあるか

---

### スクリーンショット3: `history` の中身（1件でOK）

**撮り方：**

1. Firestore の「データ」タブで、**`history`** コレクションをクリックして開く
2. 履歴ドキュメントが1件以上あれば、そのうち **1件** をクリックして開く
3. そのドキュメントの **フィールド名の一覧**（`createdAt`, `createdAtOrder`, `asin`, `status` など）が分かるようにスクリーンショットを撮る  
   - 値は隠してOK

**確認したいこと：**  
`createdAtOrder` があるか（並び順に必須）。そのほか、アプリが使っているフィールド名と揃っているか

---

## 送るときのポイント

- **コレクション名**・**ドキュメントID**・**フィールド名** が読めれば、こちらで「コードと一致しているか」を判断できます。
- **フィールドの値**（ASIN・商品名・数値など）は写さなくて大丈夫です。個人情報や業務データを避けるため、値は隠して送ってください。
- 上記の3枚が揃うと、「Firebase に上がっている情報」と「アプリが想定している形」が同じかどうかを、具体的に回答できます。

---

## 補足：シークレットモードと通常モードで履歴が違う理由

- **通常モード**：ログイン済み → Firestore の履歴を表示
- **シークレットモード**：未ログイン（別セッション）→ そのブラウザの localStorage のみ表示（Firestore には接続しない）

つまり「シェアできていない」ように見えるのは、**シークレットではログインしていないため、Firestore の履歴が表示されていない**状態です。  
第三者にも **同じURLでログイン** してもらえば、同じ Firestore の履歴（利益の画面）が見えるはずです。

上記のスクリーンショットで、Firestore 側の構造がコードと一致しているかを確認すると、さらに安心して共有設定を進められます。
