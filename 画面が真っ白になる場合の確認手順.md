# 画面が真っ白になる場合の確認手順

画面に何も表示されない（真っ白）ときは、**JavaScript のエラー**でアプリが止まっていることがほとんどです。次の順で確認してください。

---

## ステップ1: ブラウザの「コンソール」でエラーを確認する

1. ブラウザで **http://localhost:3000** を開いた状態にします。
2. キーボードの **F12** を押す（または画面で右クリック → **「検証」**）。
3. 開いたウィンドウの上にある **「Console」**（コンソール）タブをクリックします。
4. **赤い文字** で表示されているエラーを探します。
   - 例: `Failed to compile` / `Cannot find module 'firebase'` / `useAuth は AuthProvider 内で...` など

**ここに表示されているエラーの内容** が、原因を特定する手がかりになります。

---

## ステップ2: よくある原因と対処

### ① 「Cannot find module 'firebase'」や「Module not found」と出る

**原因:** Firebase のパッケージが入っていない。

**対処:**

1. Cursor で **ターミナル** を開く（メニュー → ターミナル → 新しいターミナル）。
2. 次を実行する。

```powershell
cd c:\Users\KH060\amazon-profit-calculator
npm install
```

3. 終わったら、もう一度 **`npm start`** で起動する。

---

### ② Firebase の初期化や「Invalid API key」などのエラー

**原因:** `.env` の値が間違っている、または不足している。

**対処:**

1. プロジェクトの **一番上のフォルダ** に **`.env`** があるか確認する。
2. 中身が次の **6行** になっているか確認する（値はあなたの Firebase のものに置き換える）。

```
REACT_APP_FIREBASE_API_KEY=...
REACT_APP_FIREBASE_AUTH_DOMAIN=...
REACT_APP_FIREBASE_PROJECT_ID=...
REACT_APP_FIREBASE_STORAGE_BUCKET=...
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=...
REACT_APP_FIREBASE_APP_ID=...
```

3. **注意:**
   - 値の前後に **スペース** を入れない。
   - 値の前後に **ダブルクォート " "** を付けない（付けているとエラーになることがあります）。
4. 保存したら、ターミナルで **Ctrl + C** で一度止め、**`npm start`** で再度起動する。

---

### ③ いったん Firebase をやめて、以前のように動かす

**原因:** Firebase 周りでエラーが出ていて、アプリ全体が止まっている。

**対処:** 一時的に Firebase を使わないようにする。

1. プロジェクトの一番上にある **`.env`** を **リネーム** する（例: `.env.backup`）。
2. または `.env` を開き、**6行すべての先頭に `#` を付けてコメントアウト** する。

   ```
   #REACT_APP_FIREBASE_API_KEY=...
   #REACT_APP_FIREBASE_AUTH_DOMAIN=...
   ...
   ```

3. 保存してから、ターミナルで **Ctrl + C** で止め、**`npm start`** で再度起動する。
4. これで **ログインなし・ローカルのみ** の従来の画面になるはずです。  
   問題なく表示されれば、原因は Firebase の設定側にあります。

---

## ステップ3: エラー表示が出るようにした場合

コード側で **エラー境界** を入れています。  
何か予期しないエラーで止まると、「エラーが発生しました」というメッセージと簡単な内容が表示されます。

- それでも **真っ白のまま** のときは、**ステップ1** のコンソールの **赤いエラー** を必ず確認してください。
- 表示されたエラー文をそのままコピーして、サポートや開発者に伝えると原因を特定しやすくなります。

---

## まとめ

1. **F12** → **「Console」** で **赤いエラー** を確認する。
2. **`npm install`** を実行し、**`npm start`** で再起動する。
3. **`.env`** の書き方（スペース・クォート・6行）を確認し、必要なら修正して再起動する。
4. それでもダメなときは **.env を無効化** して、Firebase なしで動くか確認する。

エラーの内容が分かれば、より具体的な対処ができます。
