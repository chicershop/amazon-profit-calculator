# Firebase の設定手順

共有データと承認フローを使うには、Firebase プロジェクトの作成とアプリ側の設定が必要です。

---

## 1. Firebase プロジェクトを作成する

1. https://console.firebase.google.com/ にアクセスし、Googleアカウントでログインします。
2. **「プロジェクトを追加」** をクリックし、プロジェクト名（例: amazon-profit-calculator）を入力して作成します。
3.  Google アナリティクスの有無はどちらでも構いません。

---

## 2. アプリを登録する

1. プロジェクトの **「プロジェクトの設定」**（歯車アイコン）→ **「一般」** を開きます。
2. **「マイアプリ」** で **「</>」（ウェブ）** をクリックし、アプリのニックネームを入力して **「アプリを登録」** します。
3. 表示された **firebaseConfig** の値をメモします（apiKey, authDomain, projectId, storageBucket, messagingSenderId, appId）。

---

## 3. 認証を有効にする

1. 左メニュー **「Build」** → **「Authentication」** を開きます。
2. **「始める」** をクリックし、**「メール/パスワード」** を有効にします（上の方の「メール/パスワード」の横の有効化スイッチをオンにします）。

---

## 4. Firestore を作成する

1. 左メニュー **「Build」** → **「Firestore Database」** を開きます。
2. **「データベースを作成」** をクリックし、**本番モード** で作成します（あとでルールを変更します）。
3. リージョンは **asia-northeast1（東京）** を推奨します。

---

## 5. Firestore のセキュリティルールを設定する

1. Firestore の **「ルール」** タブを開きます。
2. このリポジトリの **`firestore.rules`** の内容をコピーして貼り付け、**「公開」** します。

---

## 6. 環境変数を設定する

1. プロジェクトのルートに **`.env`** ファイルを作成します（存在しない場合）。  
   **`.env.example`** をコピーして **`.env`** としても構いません。
2. 次のように、Firebase の設定値を入れます。

```
REACT_APP_FIREBASE_API_KEY=あなたのapiKey
REACT_APP_FIREBASE_AUTH_DOMAIN=あなたのprojectId.firebaseapp.com
REACT_APP_FIREBASE_PROJECT_ID=あなたのprojectId
REACT_APP_FIREBASE_STORAGE_BUCKET=あなたのprojectId.appspot.com
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=数字のsenderId
REACT_APP_FIREBASE_APP_ID=1:数字:web:英数字
```

3. **`.env`** は Git にコミットしないでください（すでに `.gitignore` に含まれている想定です）。

---

## 7. アプリを起動する

1. ターミナルで `npm install` を実行し、`npm start` で起動します。
2. ブラウザで開き、**ログイン** または **新規登録** でメールアドレスとパスワードを入力します。
3. 初回ログイン時は **「このアカウントをオーナーに設定」** または **「スキップしてアプリを開く」** を選びます。  
   **オーナー** に設定したアカウントだけが、承認待ちのリサーチを **承認** できます。

---

## 使い方の流れ

- **相手（第三者）:** 同じURLでアプリを開き、別のメールで新規登録またはログインします。リサーチを入力し、**「保存（後で判定）」** や **「完了」** を押すと、その内容はクラウドに保存され、**承認待ち** などのステータスで一覧に表示されます。
- **あなた（オーナー）:** 履歴タブで **「承認待ち」** を選ぶと、相手が保存したリサーチが一覧に出ます。内容を確認し、**「承認」** ボタンを押すとステータスが **完了** になります。
- **.env を設定しない場合:** 従来どおり、この端末のブラウザ内（LocalStorage）だけに保存され、共有はされません。
