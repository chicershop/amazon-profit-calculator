# アップデートをブラウザに反映する手順

コードの変更はすでに保存されています。**ブラウザで最新の表示にするには、開発サーバーを起動する必要があります。**

---

## ステップ1: プロジェクトのフォルダを開く

1. **Cursor**（または VS Code）で、このプロジェクト `amazon-profit-calculator` を開いた状態にします。
2. 左のファイル一覧で、**一番上に `amazon-profit-calculator` というフォルダ**があることを確認してください。

---

## ステップ2: ターミナルを開く

1. 画面上部のメニューから **「ターミナル」** をクリックします。
2. 一覧から **「新しいターミナル」** を選びます。
3. 画面下（または横）に、**黒い画面（ターミナル）** が表示されます。

---

## ステップ3: プロジェクトのフォルダに移動する（必要な場合）

ターミナルに表示されているパスの**最後**が `amazon-profit-calculator` になっているか確認します。

- **なっている場合** → そのままステップ4へ。
- **なっていない場合** → 次のコマンドを**1行ずつ**ターミナルに打ち込んで Enter を押します。

```bash
cd c:\Users\KH060\amazon-profit-calculator
```

（パスは、Cursor で開いているフォルダの実際のパスに合わせて変更してください。）

---

## ステップ4: 開発サーバーを起動する

1. ターミナルに、次のコマンドを**そのまま**打ち込みます。

```bash
npm start
```

2. **Enter キー**を押します。
3. **初回だけ**「依存関係がありません。インストールしますか？」のようなメッセージが出た場合は、**y** と入力して Enter を押します。
4. 数十秒〜1分ほど待つと、次のような表示になります。
   - `Compiled successfully!`
   - ブラウザが自動で開き、**http://localhost:3000** のページが表示されることがあります。

---

## ステップ5: ブラウザでサイトを開く・更新する

### ブラウザが自動で開いた場合

- 表示されているページが **Amazon利益計算ツール** なら、そのまま使えます。
- 古い画面のままの場合は、**Ctrl + Shift + R**（Mac の場合は **Command + Shift + R**）で**強制再読み込み**してください。

### ブラウザが自動で開かなかった場合

1. **Chrome** や **Edge** などのブラウザを開きます。
2. アドレス欄に次のURLを入力して Enter を押します。

```
http://localhost:3000
```

3. **Amazon利益計算ツール**の画面が表示されればOKです。

---

## アップデートが反映されているか確認する

次のどれかができていれば、アップデートが反映されています。

- **利益計算**画面で、**FBA配送代行手数料**が「〇〇円（〇〇・自動）」のように**自動計算で表示**されている。
- **チャットワーク依頼テキスト**の画面に **「特記する仕様」** の入力欄がある。
- **詳細リサーチ**の画面に **「1688購入サイトURL」** と **「リサーチ実行」** ボタンがある。
- **設定**画面に **「チャットワーク連携」**（APIトークン・ルームID）の欄がある。

---

## うまくいかないとき

### 「npm が見つかりません」と出る

- **Node.js** が入っていない可能性があります。
- https://nodejs.org/ から **LTS 版**をダウンロードしてインストールし、**Cursor をいったん終了してから開き直し**、もう一度ステップ4からやり直してください。

### 起動中にエラーが出る

- ターミナルに表示された**エラー文**をそのままコピーして、開発者やサポートに伝えると原因を特定しやすくなります。

### 開発サーバーを止めたいとき

- ターミナルで **Ctrl + C** を押すと、サーバーが止まります。もう一度 `npm start` を実行すると、また起動します。

---

## まとめ

1. Cursor でプロジェクトを開く  
2. **ターミナル** → **新しいターミナル**を開く  
3. `cd c:\Users\KH060\amazon-profit-calculator`（必要なら）  
4. `npm start` を実行してコンパイルが終わるまで待つ  
5. ブラウザで **http://localhost:3000** を開く（または自動で開いたページを **Ctrl+Shift+R** で再読み込み）  

ここまでできていれば、アップデートは完了しています。
