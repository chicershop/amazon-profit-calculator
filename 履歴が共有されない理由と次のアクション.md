# 履歴が共有されない理由と次のアクション

## 現状の仕組み（コードで確認した内容）

- **履歴の出し分け**は `App.jsx` で次のように決まっています。
  - **Firestore の履歴**が使われる条件：**Firebase が有効** かつ **ログインしている**
  - 上記以外のときは **ブラウザの localStorage の履歴**（端末ごと・ブラウザごと）が使われます。

```javascript
// App.jsx 43行目付近
const historyApi = isFirebaseEnabled && user ? firestoreHistory : localHistory;
```

- Firestore 側では、**ユーザーで絞り込んでいません**。認証済みなら全員が**同じ `history` コレクション**を参照する設計です。  
  → 同じ Firebase プロジェクトにログインしていれば、本来は同じ履歴が見えるはずです。

---

## 履歴が「あなた」と「第三者」で違って見える主な理由（想定）

次のどれか（または組み合わせ）で、片方だけ localStorage 履歴を見ている可能性が高いです。

1. **第三者がログインしていない**  
   - 未ログインだと `user` が null のため、**常に localStorage の履歴**だけが表示されます。  
   - Firestore の履歴は「ログインしている人だけ」が見る設計です。

2. **共有しているサイト（例：Vercel）で Firebase が有効になっていない**  
   - 本番URLでは `REACT_APP_FIREBASE_*` の環境変数が未設定だと、`isFirebaseEnabled` が false になります。  
   - その場合、**そのURLで開いた人全員**が localStorage の履歴だけを見ます。  
   - あなたがローカル（.env で Firebase 設定済み）で見ていると Firestore の履歴、第三者が本番URLだけを見ていると「そのブラウザの localStorage だけ」で内容が一致しません。

3. **見ているURLが違う**  
   - あなた：`localhost`（.env で Firebase 接続）  
   - 第三者：Vercel の本番URL（ここで環境変数未設定なら Firebase 無効）  
   → 参照している「データの場所」がそもそも違う（Firestore vs ローカル）になります。

4. **参照している Firebase プロジェクトが違う**  
   - あなたの PC の `.env` と、Vercel（または他のホスト）の環境変数で、**別の Firebase プロジェクト**を指定していると、データベースが分かれるため履歴も一致しません。

---

## 次のアクション（シェアを実現するための手順）

### ステップ1: 同じ「本番URL」で同じ Firebase を使っているか確認する

- 第三者には、**あなたがデプロイした同じURL**（例：`https://あなたのプロジェクト.vercel.app`）だけを開いてもらってください。  
- あなた自身も、履歴を「共有されたもの」として確認するときは、**同じ本番URL**で開き、ローカル（localhost）ではなく本番で確認するとよいです。

### ステップ2: 本番環境で Firebase が有効か確認する

- Vercel を使っている場合：  
  - Vercel のプロジェクト → **Settings → Environment Variables** で、次の **6つ** が**本番（Production）**に設定されているか確認してください。  
    - `REACT_APP_FIREBASE_API_KEY`  
    - `REACT_APP_FIREBASE_AUTH_DOMAIN`  
    - `REACT_APP_FIREBASE_PROJECT_ID`  
    - `REACT_APP_FIREBASE_STORAGE_BUCKET`  
    - `REACT_APP_FIREBASE_MESSAGING_SENDER_ID`  
    - `REACT_APP_FIREBASE_APP_ID`  
  - 1つでも欠けていると、本番では Firebase が無効になり、履歴は Firestore ではなく **localStorage のみ**になります。  
- 設定を変更したあとは、**再デプロイ**すると確実です。

### ステップ3: 第三者が「ログイン」しているか確認する

- 履歴を Firestore から表示するには**ログインが必須**です。  
- 第三者に次を確認してもらってください。  
  - 共有されたURLを開いたとき、**ログイン画面が出ているか**  
  - **メール/パスワードでサインアップまたはログイン**しているか  
- まだアカウントがない場合は、**同じ Firebase プロジェクト**でサインアップしてもらう必要があります（あなたが Firebase コンソールで「メール/パスワード」認証を有効にしている前提）。  
- ログインしていない状態では、どんなに Firestore にデータがあっても、アプリ上では **localStorage の履歴だけ**が表示されます。

### ステップ4: 同じ Firebase プロジェクトを参照しているか確認する

- あなたの `.env` の `REACT_APP_FIREBASE_PROJECT_ID` と、Vercel（または本番用）の環境変数 `REACT_APP_FIREBASE_PROJECT_ID` が**完全に同じ値**か確認してください。  
- 違うプロジェクトだと、Firestore のデータが別々になり、履歴は共有されません。

---

## チェックリスト（共有がうまくいかないとき）

| 確認項目 | あなた側 | 第三者側 |
|----------|----------|----------|
| 同じ本番URL（例：Vercel）を開いているか | 本番URLで確認 | 共有されたURLのみ使用 |
| 本番環境に Firebase の環境変数が6つ入っているか | Vercel の設定で確認 | - |
| ログインしているか | ログイン済み | **ログイン必須** |
| 参照している Firebase プロジェクトが同じか | .env と Vercel の PROJECT_ID が一致 | - |

---

## まとめ

- **Firestore にアップロードされている**のは正しくても、**アプリが Firestore の履歴を使う条件**は「**Firebase が有効な環境**」かつ「**ログイン済み**」のときだけです。  
- 第三者が「ログインしていない」か、または「Firebase が無効なURL（環境変数未設定の本番）だけを見ている」と、その人には **localStorage の履歴**しか表示されず、あなたの画面と内容が異なります。  
- 上記の「次のアクション」のとおり、**同じ本番URL・同じ Firebase 設定・第三者もログイン**を満たすと、同じ Firestore の履歴が二人とも見られるようになります。
