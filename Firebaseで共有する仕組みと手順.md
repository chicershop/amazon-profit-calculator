# Firebase で共有する仕組みと手順

「違うパソコンを使っている第三者と共有する」ために Firebase を入れた、という理解で正しいです。  
**何が Firebase に上がるか** と **第三者が使うまでに必要なこと** を整理します。

---

## 何が Firebase にアップロードされてシェアされるか

Firebase にアップロードされ、共有されるのは **「データ」** です。アプリのプログラムそのものではありません。

| 役割 | 説明 |
|------|------|
| **Firestore（データベース）** | 利益計算の履歴・詳細リサーチ・承認待ち／完了などの **データ** がここに保存されます。あなたも第三者も、**同じ Firestore を参照**するので、**同じ一覧・同じデータ**が見えます。 |
| **Authentication（認証）** | 誰がログインしているかを管理します。あなたと第三者が **別々のメールでログイン** し、**オーナー** だけが「承認」ボタンを押せるようにしています。 |

つまり、

- **「情報を Firebase にアップロードしてシェアする」** = 履歴やリサーチの **データ** が Firestore に保存され、あなたと第三者の **両方から同じデータが見える・操作できる** 状態にした、という意味で合っています。

---

## 第三者が「アプリそのもの」にアクセスするには

データは Firebase にありますが、**第三者が「サイトを開く」ためには、そのサイトがインターネット上のどこかで動いている必要**があります。

| 役割 | 説明 |
|------|------|
| **あなたのパソコンの localhost** | あなたが `npm start` で起動している間だけ、**あなたのパソコン**から http://localhost:3000 で開けます。**第三者のパソコンからは開けません。** |
| **公開URL（例: Vercel）** | アプリを **GitHub にプッシュ → Vercel がビルドして公開** すると、https://amazon-profit-calculator-virid.vercel.app のような **誰でも開けるURL** ができます。**第三者にはこのURLを共有**します。 |

整理すると:

- **Firebase** = データをクラウドに保存して、あなたと第三者が **同じデータ** を見る・操作するためのもの。
- **Vercel（や GitHub）** = アプリを **インターネット上に置いて、第三者もブラウザで開けるようにする** ためのもの。

なので、

- 「Firebase に情報をアップロードしてシェアする」＝ **データ** を Firebase で共有する … ✅ その通りです。
- その上で、「第三者が別のパソコンから使う」には、**アプリを公開URL（Vercel など）にデプロイし、そのURLを共有する** 必要があります。

---

## 第三者と共有して使うまでの流れ（あなたがやること）

### 1. Firebase の設定（すでにやった前提）

- Firebase コンソールでプロジェクト作成
- メール/パスワード認証を有効化
- Firestore を作成し、ルールを設定
- プロジェクトの `.env` に Firebase の設定値を入れる

（まだの場合は「Firebase設定ガイド（初心者向け）.md」のとおりに実施。）

---

### 2. アプリを公開URLにデプロイする（第三者に「リンク」を渡すため）

第三者が **別のパソコンから** アプリを開くには、**Vercel のURL** のような「誰でも開けるURL」が必要です。

1. あなたのパソコンで、最新のコードを **GitHub にプッシュ** します。

   ```powershell
   cd c:\Users\KH060\amazon-profit-calculator
   git add .
   git commit -m "Firebaseで共有対応"
   git push origin main
   ```

2. すでに Vercel と GitHub を連携している場合は、数分で **本番URL**（例: https://amazon-profit-calculator-virid.vercel.app）が更新されます。
3. **この本番URL** を第三者に伝えます。「このURLをブラウザで開いて、メールとパスワードで新規登録（またはログイン）して使ってください」と案内します。

※ Vercel 側で **環境変数** も設定する必要があります。  
Firebase を使うため、Vercel のダッシュボードで次の変数を追加します。

- `REACT_APP_FIREBASE_API_KEY`
- `REACT_APP_FIREBASE_AUTH_DOMAIN`
- `REACT_APP_FIREBASE_PROJECT_ID`
- `REACT_APP_FIREBASE_STORAGE_BUCKET`
- `REACT_APP_FIREBASE_MESSAGING_SENDER_ID`
- `REACT_APP_FIREBASE_APP_ID`

値は、あなたの `.env` に入れているものと同じにします。

---

### 3. あなたと第三者の使い方

| あなた（オーナー） | 第三者 |
|--------------------|--------|
| 1. 本番URL（Vercel）をブラウザで開く | 1. あなたが共有した **同じ本番URL** をブラウザで開く |
| 2. メール/パスワードでログイン（初回は「オーナーに設定」） | 2. **別のメール** で新規登録してログイン |
| 3. 利益計算・履歴・承認などを行う | 3. 利益計算・詳細リサーチなどを入力し、「保存（後で判定）」や「完了」で保存 |
| 4. 履歴タブで「承認待ち」を選び、第三者が保存したものを確認して **「承認」** | 4. 保存した内容は Firestore に送られ、あなたの画面にも同じ一覧として表示される |

データはすべて **Firebase（Firestore）** に保存されるので、**違うパソコン同士でも同じデータを共有** でき、あなたが承認する流れで使えます。

---

## まとめ

- **Firebase に情報をアップロードしてシェアする** = 履歴・リサーチなどの **データ** を Firestore に保存し、あなたと第三者が **同じデータ** を見て操作する … この理解で正しいです。
- そのために、**あなたがやること** は次の2つです。
  1. **Firebase の設定**（プロジェクト・認証・Firestore・.env）を済ませる。
  2. **アプリを公開URL（Vercel など）にデプロイし、そのURLを第三者に共有する**。あわせて Vercel の環境変数に Firebase の設定を入れる。

こうすると、第三者は **別のパソコン** から共有されたURLでアプリを開き、ログインして使え、入力した内容は Firebase に保存されてあなたの画面にも表示され、あなたが承認できます。
