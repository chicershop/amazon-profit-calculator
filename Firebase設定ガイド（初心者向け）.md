# Firebase 設定ガイド（初心者向け）

「amazon-profit-calculator」の Firebase プロジェクトは**すでに作成できています**。  
このあと、**4つの作業**を順番にやると、相手とデータを共有して承認できるようになります。

---

## やること一覧

| 番号 | やること | どこでやる |
|------|----------|------------|
| **1** | ウェブアプリを Firebase に登録し、設定値をメモする | Firebase コンソール（ブラウザ） |
| **2** | ログイン機能（メール/パスワード）を有効にする | Firebase コンソール |
| **3** | データ保存用の Firestore を作成し、ルールを貼る | Firebase コンソール |
| **4** | メモした設定値をプロジェクトの `.env` に書き、アプリを起動する | Cursor（自分のパソコン） |

以下、**1 → 2 → 3 → 4** の順で、画面のどこを押すかまで書きます。

---

# 作業1: ウェブアプリを登録して「設定値」をメモする

## 1-1. プロジェクトの設定を開く

1. Firebase コンソール（https://console.firebase.google.com/）で、**「amazon-profit-calculator」** を開いた状態にします。
2. 画面左上の **歯車アイコン** をクリックします。
3. 一覧から **「プロジェクトの設定」** をクリックします。

## 1-2. ウェブアプリを追加する

1. **「プロジェクトの設定」** の画面で、下の方にある **「マイアプリ」** という欄を探します。
2. その中に **「</>」**（ウェブのアイコン）があるので、**クリック**します。
3. **「アプリのニックネーム」** に、例えば **`amazon-calc-web`** と入力します（何でもよいです）。
4. **「アプリを登録」** ボタンをクリックします。
5. 次の画面で **「firebaseConfig」** というコードが表示されます。  
   この中の **次の6つ** をメモ帳などにコピーしておきます。  
   （値は人それぞれなので、あなたの画面に表示されているものをそのまま使います。）

   - **apiKey** の `"..."` の中身
   - **authDomain** の `"..."` の中身
   - **projectId** の `"..."` の中身
   - **storageBucket** の `"..."` の中身
   - **messagingSenderId** の `"..."` の中身（数字）
   - **appId** の `"..."` の中身

6. **「次のステップ」** などは押さずに、このままコンソールの左メニューに進んで大丈夫です。

---

# 作業2: ログイン機能（メール/パスワード）を有効にする

## 2-1. Authentication を開く

1. Firebase コンソールの **左側のメニュー** を確認します。
2. **「Build」**（または「構築」）の **▶** をクリックして開きます。
3. 一覧から **「Authentication」**（認証）をクリックします。

## 2-2. 認証を「始める」

1. **「始める」** というボタンがあれば、それをクリックします。
2. すでに始めている場合は、そのまま次の 2-3 へ。

## 2-3. メール/パスワードを有効にする

1. **「Sign-in method」**（サインイン方法）というタブをクリックします。
2. 一覧に **「メール/パスワード」** があるので、**その行をクリック**します。
3. **「メール/パスワード」** の右側にある **スイッチをオン**（有効）にします。
4. **「保存」** をクリックします。

これで「メールアドレスとパスワードでログインする」機能が使えるようになります。

---

# 作業3: Firestore（データ保存）を作成してルールを貼る

## 3-1. Firestore を開く

1. 左メニューの **「Build」** の下にある **「Firestore Database」** をクリックします。

## 3-2. データベースを作成する

1. **「データベースを作成」** ボタンをクリックします。
2. **「本番モードで開始」** を選び、**「次へ」** をクリックします。
3. **ロケーション（リージョン）** で、できるだけ **「asia-northeast1」（東京）** を選びます。
4. **「有効にする」** をクリックします。
5. データベースができるまで少し待ちます。

## 3-3. セキュリティルールを貼り替える

1. Firestore の画面で、上の方にある **「ルール」** タブをクリックします。
2. すでに書かれているルール（`rules_version = '2';` から始まる文）を **すべて選択** して **削除** します。
3. このプロジェクトのフォルダにある **`firestore.rules`** というファイルを、Cursor で開きます。
4. その中身を **すべてコピー** します。
5. Firebase コンソールの **「ルール」** の入力欄に **貼り付け** ます。
6. **「公開」** ボタンをクリックします。

これで「ログインした人だけがデータの読み書きできる」ルールになります。

---

# 作業4: 自分のパソコンで .env を作り、アプリを動かす

## 4-1. .env ファイルを作る

1. **Cursor** で、**amazon-profit-calculator** のプロジェクトを開きます。
2. 左のファイル一覧で、**一番上（プロジェクトの根っこ）** を確認します。  
   `package.json` や `src` フォルダがある場所です。
3. その場所に、**新しいファイル** を作ります。  
   - ファイル名は **`.env`** にします（先頭のドットを忘れずに）。
4. その `.env` に、次の **6行** を書きます。  
   **「＝」の右側** に、作業1でメモした **あなたの値** を入れます。  
   値の前後に **ダブルクォート " " は入れません**。

```
REACT_APP_FIREBASE_API_KEY=ここにapiKeyの値
REACT_APP_FIREBASE_AUTH_DOMAIN=ここにauthDomainの値
REACT_APP_FIREBASE_PROJECT_ID=ここにprojectIdの値
REACT_APP_FIREBASE_STORAGE_BUCKET=ここにstorageBucketの値
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=ここにmessagingSenderIdの値
REACT_APP_FIREBASE_APP_ID=ここにappIdの値
```

**例（あなたの値に置き換えてください）:**

```
REACT_APP_FIREBASE_API_KEY=AIzaSyxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
REACT_APP_FIREBASE_AUTH_DOMAIN=amazon-profit-calculator.firebaseapp.com
REACT_APP_FIREBASE_PROJECT_ID=amazon-profit-calculator
REACT_APP_FIREBASE_STORAGE_BUCKET=amazon-profit-calculator.appspot.com
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=123456789012
REACT_APP_FIREBASE_APP_ID=1:123456789012:web:abcdef123456
```

5. **保存** します（Ctrl + S）。

## 4-2. パッケージを入れてアプリを起動する

1. Cursor の **「ターミナル」** を開きます（メニュー → ターミナル → 新しいターミナル）。
2. 次のコマンドを **1行ずつ** 入力して Enter を押します。

```powershell
cd c:\Users\KH060\amazon-profit-calculator
```

```powershell
npm install
```

```powershell
npm start
```

3. 数十秒待つと、ブラウザが開き **「Amazon利益計算ツール」** の画面が出ます。  
   **Firebase を正しく設定できていると、最初に「ログイン」の画面** が表示されます。

## 4-3. 動作確認

1. ログイン画面で **「新規登録する」** をクリックします。
2. メールアドレスとパスワード（6文字以上）を入力し、**「新規登録」** をクリックします。
3. 次に **「このアカウントをオーナーに設定」** または **「スキップしてアプリを開く」** が出たら、どちらか選びます。
4. 利益計算の画面が出れば、**設定は完了** です。

---

# うまくいかないとき

- **ログイン画面が出ず、いきなり利益計算の画面が出る**  
  → `.env` の名前や場所が間違っていないか確認してください（プロジェクトの一番上のフォルダに `.env`）。  
  → 値を貼り付けたあと、**余計なスペースやダブルクォート** が入っていないか確認し、保存し直してから **もう一度 `npm start`** してください。

- **「Firebase が無効です」や「Permission denied」と出る**  
  → 作業2（メール/パスワードを有効にする）と 作業3（Firestore のルールを貼る）を、もう一度手順どおりにやってください。

- **.env を保存したのに反映されない**  
  → ターミナルで **Ctrl + C** で一度 `npm start` を止め、もう一度 **`npm start`** を実行してください。

---

# まとめ（何をしているか）

| 作業 | やっていること |
|------|----------------|
| **1** | Firebase に「このウェブアプリ」を登録し、接続に必要な6つの値を取得している。 |
| **2** | 「メールとパスワードでログインする」機能をオンにしている。 |
| **3** | データを保存する「Firestore」を作り、「ログインした人だけ読み書きできる」ルールにしている。 |
| **4** | 取得した6つの値を `.env` に書き、アプリから Firebase に接続できるようにして、アプリを起動している。 |

ここまでできていれば、**あなたと相手がそれぞれログインして、同じデータを見て・承認する** 流れが使える状態になっています。
