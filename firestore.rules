rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 設定（オーナーUID）: 認証済みなら読み書き可
    match /config/{doc} {
      allow read, write: if request.auth != null;
    }
    // 履歴: 認証済みなら読み書き可（共有用）
    match /history/{docId} {
      allow read, write: if request.auth != null;
    }
  }
}
