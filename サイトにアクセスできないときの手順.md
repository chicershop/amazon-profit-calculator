# サイトにアクセスできないときの手順

http://localhost:3000/ にアクセスできないときの「いまの状況」と、あなたがやるタスクをまとめます。

---

## いまの状況（なぜアクセスできないか）

**http://localhost:3000** は「あなたのパソコンのなかで動いている開発用サーバー」のアドレスです。

| ポイント | 説明 |
|----------|------|
| **サーバーを起動していないと表示されない** | ブラウザで URL を開く**前に**、Cursor のターミナルで **`npm start`** を実行して、開発サーバーを起動する必要があります。 |
| **起動していないとき** | サーバーが動いていないと、localhost:3000 には何もないので「このサイトにアクセスできません」「接続できません」のような表示になります。 |
| **起動しているとき** | ターミナルで `npm start` を実行したあと、同じパソコンのブラウザで http://localhost:3000 を開くと、Amazon利益計算ツールの画面が表示されます。 |
| **ほかの人からは見られない** | localhost は「このパソコンだけ」のアドレスなので、相手のパソコンやスマホから http://localhost:3000 でアクセスすることはできません。相手に見てもらうには、GitHub + Vercel などにデプロイした「本番のURL」を共有する必要があります。 |

**まとめ:**  
「アクセスできない」多くの場合は **「開発サーバー（npm start）をまだ起動していない」** か **「一度止まってしまった」** 状態です。  
まず、**あなたのパソコンで開発サーバーを起動する** ことが最初のタスクです。

---

## あなたがやるタスク（順番に）

### タスク1: プロジェクトを Cursor で開く

1. **Cursor** を起動します。
2. **ファイル** → **フォルダーを開く** から、次のフォルダを選びます。  
   `c:\Users\KH060\amazon-profit-calculator`
3. 左のファイル一覧に `package.json` や `src` フォルダが見えていればOKです。

---

### タスク2: ターミナルを開く

1. 画面上部のメニューから **「ターミナル」** をクリックします。
2. **「新しいターミナル」** を選びます。
3. 画面の下（または横）に、**黒い画面（ターミナル）** が表示されます。

---

### タスク3: プロジェクトのフォルダに移動する

1. ターミナルに、次の **1行** をそのまま貼り付けて **Enter** を押します。

```powershell
cd c:\Users\KH060\amazon-profit-calculator
```

2. 次のタスクで `npm start` を打つとき、**このフォルダにいる状態** にしておきます。

---

### タスク4: 開発サーバーを起動する

1. ターミナルに、次の **1行** を入力して **Enter** を押します。

```powershell
npm start
```

2. **数十秒〜1分** ほど待ちます。
3. **「Compiled successfully!」** のような表示が出たら成功です。
4. ブラウザが自動で開いて **http://localhost:3000** が表示されることがあります。  
   開かない場合は、次のタスク5へ進みます。

**注意:**  
このターミナルは **閉じないでください**。閉じるとサーバーが止まり、またアクセスできなくなります。

---

### タスク5: ブラウザでサイトを開く

1. **Chrome** や **Edge** など、ブラウザを開きます。
2. アドレス欄に次のURLを入力して **Enter** を押します。

```
http://localhost:3000
```

3. **Amazon利益計算ツール** の画面（ログイン画面または利益計算の画面）が表示されれば、アクセスできています。

---

### タスク6: 次回以降、またアクセスしたいとき

- **同じ手順** で大丈夫です。  
  **タスク2** でターミナルを開く → **タスク3** で `cd c:\Users\KH060\amazon-profit-calculator` → **タスク4** で `npm start` → **タスク5** でブラウザから http://localhost:3000 を開く。
- Cursor を終了したり、パソコンを再起動したりすると、開発サーバーは止まります。そのときは、もう一度 **タスク2〜5** からやり直します。

---

## うまくいかないときの確認

| 状況 | やること |
|------|----------|
| **「npm は認識されていません」と出る** | Node.js が入っていません。https://nodejs.org/ から **LTS版** をインストールし、Cursor を一度終了してから開き直し、もう一度タスク3から試します。 |
| **「Compiled successfully」が出ない・赤いエラーが出る** | ターミナルに表示されている **赤いエラー文** をコピーして、その内容をメモしておきます。エラーに合わせた対処が必要です。 |
| **ブラウザで「このサイトにアクセスできません」** | タスク4の **`npm start`** を実行したか確認します。実行したターミナルを閉れていないか、**Compiled successfully** まで表示されているかも確認します。 |
| **画面が真っ白** | ブラウザで **F12** を押し、**「Console」** タブで赤いエラーが出ていないか確認します。出ていれば、その内容をメモしておくと原因を特定しやすくなります。 |

---

## 相手にURLを共有して使ってもらう場合

- **http://localhost:3000** は、**あなたのパソコンで開発サーバーを起動しているときだけ**、あなたのパソコンから開けるアドレスです。
- **相手** が同じツールにアクセスするには、**本番用のURL** が必要です。  
  例: 以前使っていた **https://amazon-profit-calculator-virid.vercel.app** のような、GitHub にプッシュすると Vercel が自動で更新してくれるURLです。
- やることの流れは次のとおりです。  
  1. あなたのパソコンでコードを編集・保存する  
  2. ターミナルで `git add .` → `git commit -m "メッセージ"` → `git push origin main` を実行する  
  3. 数分待つと、Vercel の本番URL（例: amazon-profit-calculator-virid.vercel.app）が更新される  
  4. **その本番URL** を相手に共有する  

相手に見てもらうときは、**localhost のURL ではなく、この本番URLを共有** してください。

---

## まとめ（あなたがやるタスク一覧）

| 番号 | タスク | やること |
|------|--------|----------|
| 1 | プロジェクトを開く | Cursor で `c:\Users\KH060\amazon-profit-calculator` を開く |
| 2 | ターミナルを開く | メニュー「ターミナル」→「新しいターミナル」 |
| 3 | フォルダに移動 | ターミナルに `cd c:\Users\KH060\amazon-profit-calculator` と入力して Enter |
| 4 | サーバーを起動 | ターミナルに `npm start` と入力して Enter。「Compiled successfully」まで待つ |
| 5 | ブラウザで開く | ブラウザのアドレス欄に `http://localhost:3000` と入力して Enter |
| 6 | 次回以降 | またアクセスしたいときは、タスク2〜5を同じように繰り返す |

ここまでできていれば、**あなたのパソコンでは** http://localhost:3000 でサイトにアクセスできる状態になります。
