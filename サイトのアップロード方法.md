# サイトのアップロード方法

このドキュメントでは、Amazon利益計算ツールを本番用にビルドし、インターネット上に公開する手順を説明します。

---

## ステップ1: 本番用にビルドする

アップロードする前に、**本番用のファイル**を作成します。

### 1-1. ターミナルを開く

- Cursor（または VS Code）で、プロジェクト **amazon-profit-calculator** を開いた状態にします。
- メニュー **「ターミナル」** → **「新しいターミナル」** を選びます。

### 1-2. プロジェクトのフォルダに移動する

ターミナルに次のコマンドを入力して **Enter** を押します。

```powershell
cd c:\Users\KH060\amazon-profit-calculator
```

（プロジェクトを別の場所に保存している場合は、そのパスに書き換えてください。）

### 1-3. ビルドを実行する

次のコマンドを入力して **Enter** を押します。

```powershell
npm run build
```

- 数十秒〜1分ほど待ちます。
- **「Compiled successfully.」** や **「The build folder is ready to be deployed.」** のような表示が出れば成功です。

### 1-4. ビルド結果の確認

プロジェクト内に **`build`** というフォルダができます。

- この **`build`** フォルダの中身（ファイルとフォルダ一式）が、そのまま「サイト」です。
- **この `build` フォルダの中身を、あとでホスティング先にアップロード**します。

---

## ステップ2: どこにアップロードするか選ぶ

サイトを公開する場所（ホスティング）を決めます。よく使われる方法は次のとおりです。

| 方法 | 特徴 | 向いている人 |
|------|------|----------------|
| **Netlify** | 無料枠あり、ドラッグ＆ドロップで簡単 | 初めての人 |
| **Vercel** | 無料枠あり、Git連携が簡単 | GitHubを使っている人 |
| **GitHub Pages** | 無料、GitHubのリポジトリから公開 | GitHubを使っている人 |
| **レンタルサーバー / FTP** | 既存のサーバーがある | すでに契約している人 |

以下、**Netlify** と **レンタルサーバー（FTP）** の2パターンを説明します。

---

## 方法A: Netlify にアップロードする（おすすめ）

Netlify は無料で使え、**`build` フォルダをドラッグするだけ**で公開できます。

### A-1. Netlify のアカウントを作る

1. ブラウザで **https://www.netlify.com** を開きます。
2. **「Sign up」** から、メールアドレスまたは GitHub でアカウントを作成します。

### A-2. サイトをデプロイする

1. ログイン後、Netlify の画面で **「Add new site」** → **「Deploy manually」** を選びます。
2. **「Drag and drop your site output folder here」** と書かれた枠が表示されます。
3. パソコンで **`c:\Users\KH060\amazon-profit-calculator\build`** フォルダを開きます。
4. **`build` フォルダの中身（中にあるファイル・フォルダすべて）** を、Netlify の枠に**ドラッグ＆ドロップ**します。
   - 「build フォルダそのもの」ではなく、「build の中身」をドロップする方法でも大丈夫です。Netlify はどちらでも受け付ける場合があります。うまくいかない場合は「中身だけ」を試してください。
5. アップロードが終わると、**https://xxxxx.netlify.app** のようなURLが表示されます。これがあなたのサイトのアドレスです。

### A-3. 確認

表示されたURLをクリックして、Amazon利益計算ツールが正しく表示されれば完了です。

---

## 方法B: レンタルサーバー（FTP）にアップロードする

お名前.com、エックスサーバー、さくら、など「レンタルサーバー」を契約している場合は、FTPで **`build` の中身** を **公開用フォルダ** に上げます。

### B-1. FTPソフトを用意する

- **FileZilla**（無料）がよく使われます。  
  https://filezilla-project.org/ からダウンロードしてインストールします。

### B-2. サーバーに接続する

1. FileZilla を起動し、レンタルサーバーから案内されている次の情報を入力します。
   - **ホスト（FTPサーバー）**
   - **ユーザー名**
   - **パスワード**
   - **ポート**（通常は 21）
2. **「クイック接続」** で接続します。

### B-3. アップロード先を開く

- 右側（リモート側）で、**Web公開用のフォルダ** を開きます。
  - よくある名前: **`public_html`** や **`www`** や **`htdocs`**
  - サーバーの説明書で「ドキュメントルート」と書いてあるフォルダです。
- サブフォルダで公開する場合（例: `https://あなたのドメイン/amazon-calc/`）は、そのフォルダ（例: `amazon-calc`）を開きます。

### B-4. ファイルをアップロードする

1. 左側（ローカル側）で、**`c:\Users\KH060\amazon-profit-calculator\build`** を開きます。
2. **`build` の中身**（`index.html` や `static` フォルダなど）を**すべて選択**します。
3. 選択したファイル・フォルダを、右側の**公開用フォルダ**にドラッグしてアップロードします。
4. 転送が終わるまで待ちます。

### B-5. 確認

- ブラウザで **https://あなたのドメイン/** または **https://あなたのドメイン/amazon-calc/** を開きます。
- Amazon利益計算ツールが表示されれば完了です。

**注意:**  
React アプリは「トップの `index.html` だけ」で動くため、多くのレンタルサーバーでそのまま使えます。  
ただし、**サブフォルダ**（例: `/amazon-calc/`）で公開する場合は、`package.json` に `"homepage": "https://あなたのドメイン/amazon-calc"` を追加してから、もう一度 **`npm run build`** を実行する必要があります。

---

## まとめ

1. **ターミナルで** `cd c:\Users\KH060\amazon-profit-calculator` → `npm run build` を実行する。  
2. **`build` フォルダの中身**が「サイト」なので、これをアップロードする。  
3. **Netlify** なら「Deploy manually」で `build` の中身をドラッグ＆ドロップ。  
4. **レンタルサーバー**なら FTP で `build` の中身を `public_html`（または指定のフォルダ）にアップロードする。

更新したときは、もう一度 **`npm run build`** を実行し、新しくできた **`build` の中身** を同じ手順でアップロードし直すと、サイトの内容が更新されます。
