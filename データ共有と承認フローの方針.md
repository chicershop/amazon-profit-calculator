# データ共有と「相手が操作 → 私が承認」の実現方法

- **目的:** 相手が操作した内容をあなたがすべて見えるようにし、あなたが承認するフローにしたい
- **条件:** 無料枠を利用したい、利用データ量はまだ不明
- **疑問:** Googleドライブなどで保存先を代用できるか

---

## 結論：2つの選択肢

| 方法 |  Googleドライブの代用 | 無料 | すべてのデータが見える | 承認フロー | おすすめ度 |
|------|------------------------|------|--------------------------|------------|-------------|
| **A. クラウドDB（Firebase / Supabase）** | 不可（専用のDBを使う） | 〇 無料枠あり | 〇 リアルタイムで共有 | 〇 アプリ内で完結 | ★★★ 最適 |
| **B. Googleドライブで手動共有** | 〇 代用できる | 〇 無料 | 〇 取り込み後にすべて見える | 〇 アプリに「承認」を追加すれば可能 | ★★ 手軽だが手動作業あり |

**おすすめ:** データ量が不明でも無料枠で足りる **方法A（Firebase または Supabase）** を前提に設計し、その上で「承認待ち一覧」と「承認ボタン」を用意する形が最もシンプルです。

方法Bは「まずサーバーを一切増やしたくない」場合の代案です。

---

## 方法A: クラウドDB（Firebase / Supabase）で共有＋承認

### できること

- 相手が入力・保存した内容が **同じDB** に保存される
- あなたがアプリを開くと **相手のデータも含めてすべて** 表示される（リアルタイムまたは更新ボタンで反映）
- 各履歴に「承認待ち」「承認済み」などの状態を持たせ、**あなただけが「承認」ボタンを押せる** ようにする

### 無料枠（データ量が不明でも安心できる目安）

- **Firebase (Firestore)**  
  - 1GB 保存、1日あたり 5万回読み取り・2万回書き込み まで無料  
  - 小〜中規模の利用ならまず無料で足りる
- **Supabase**  
  - 500MB のDB、月間 2GB 転送 まで無料  
  - 同様に、少人数・中程度の利用なら無料で運用可能

「どれくらいデータを使うか分からない」場合でも、**まずは無料枠で始めて、増えてから有料プランや別手段を検討**する形で問題ありません。

### 実装のイメージ

1. Firebase または Supabase の無料アカウントを作成する
2. アプリ側で「履歴」「設定」の保存先を、今の LocalStorage から **クラウドのDB** に切り替える
3. 履歴データに「作成者」「ステータス（下書き / 承認待ち / 承認済み）」を持たせる
4. 相手: これまで通り入力・保存。保存時に「承認待ち」で登録
5. あなた: 履歴一覧で「承認待ち」だけフィルタ表示し、内容を確認して「承認」ボタンでステータスを「承認済み」に更新

この場合、**Googleドライブは使いません**。保存先はあくまで Firebase / Supabase のDBです。

---

## 方法B: Googleドライブで「代用」する（手動同期）

### できること

- **保存先そのもの**をGoogleドライブのAPIで置き換えることは、技術的・運用面でかなり重いため、ここでは **「データの受け渡し」だけドライブを使う** 形にします。
- 相手が **エクスポート（JSON）** → そのファイルを **共有フォルダに置く** → あなたが **ダウンロードしてアプリでインポート** する、という流れにすると、「Googleドライブで代用」できます。
- 取り込んだデータはあなたのブラウザ（LocalStorage）に入るので、**あなたの画面では相手のデータも含めてすべて見える**ようにできます。
- その上で、アプリに「承認待ち一覧」と「承認」ボタンを追加すれば、**相手が操作 → あなたが承認するフロー** は実現できます。

### 制約（ドライブで代用する場合）

- **自動同期ではない**  
  相手がエクスポートしてドライブに置き、あなたがダウンロードしてインポートする、という **手順を毎回やる** 必要があります。
- **リアルタイムではない**  
  相手が保存した直後にあなたの画面に反映されるわけではなく、「エクスポート → 共有 → インポート」のタイミングでしか見えません。
- **衝突は自分で解消**  
  同じ案件を二人が編集していると、どちらを正とするかは、インポートの仕方や運用で調整する必要があります。

### 実装のイメージ（方法Bを選ぶ場合）

1. **エクスポート**  
   すでにある「JSONでダウンロード」を、相手にも使ってもらう（履歴をJSONで保存）。
2. **共有**  
   相手はそのJSONファイルを、あなたと共有しているGoogleドライブのフォルダにアップロードする。
3. **インポート**  
   アプリに「**JSONファイルを読み込む（インポート）**」機能を追加する。  
   - あなたはドライブからJSONをダウンロードし、アプリで「ファイルを選択」してインポートする  
   - または「同じフォルダのJSONのURL」から読み込む方式（要：公開リンクとCORSなどの制約の確認）
4. **承認**  
   履歴に「承認待ち」「承認済み」を持たせ、一覧で「承認待ち」だけ表示し、あなたが「承認」ボタンで承認済みに変える。

この場合、**データの「置き場」はGoogleドライブ**ですが、**アプリの保存先は従来どおりLocalStorage**のままです。「共有」は「ファイルの受け渡しをドライブでやる」という意味での代用です。

---

## 前提条件の整理（データ量が不明な場合）

- **無料で始めたい**  
  → 方法Aも方法Bも、無料で始められます（AはFirebase/Supabaseの無料枠、Bはドライブの無料容量）。
- **すべてのデータを見たい**  
  → A: DBを見るので全件見える。B: 相手のJSONをインポートすれば、その時点の全データが見える。
- **承認フローにしたい**  
  → A: ステータスと「承認」操作をDB連携で実装。B: ステータスと「承認」をLocalStorage上で実装し、同期は「エクスポート→ドライブ→インポート」で行う。
- **データ量がまだ分からない**  
  → A: 無料枠内で始め、増えたら有料や制限対策を検討。B: ドライブの容量制限内で、JSONのサイズが増えすぎないように運用。

---

## 最適な方法の提案（まとめ）

- **「相手の操作をすべて見て、承認までアプリ内で完結させたい」** なら、**方法A（Firebase または Supabase）** が最適です。  
  - 無料枠でデータ量が不明な状態から始めても問題なく、  
  - Googleドライブは「保存先」としては使わず、クラウドDBのみで一元管理できます。

- **「いったんはサーバーや新しいサービスを増やしたくない」** 場合は、**方法B（ドライブでファイル共有＋インポート＋承認UI）** で代用できます。  
  - 無料・手軽だが、同期は手動になります。

次のステップとしては、

- **方法Aで進める** → Firebase か Supabase のどちらにするか決め、その前提で「履歴の保存先の差し替え」と「承認待ち一覧・承認ボタン」の設計に進む  
- **方法Bで進める** → 「JSONインポート」と「承認待ち一覧・承認ボタン」の仕様を決めてから実装に進む  

のどちらかで進めるとよいです。  
「クラウドの保存先をGoogleドライブで代用できるか」に対する答えは、  
**「保存先そのものは代用しないが、データの受け渡し（共有）にGoogleドライブを使う形なら代用できる。一方、共有も承認も含めて楽にしたいなら、無料枠のクラウドDB（Firebase/Supabase）が最適」** です。
